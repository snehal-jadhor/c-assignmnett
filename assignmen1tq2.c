#include<stdio.h>
int main()
{
    int area , float , circ ;
    printf("Entre the radius");
    scanf("%IF",&radius :);

printf("Entre the height");
scanf("%IF",&height);

if (radius>0, && height0>)

{
surfacearea = 2 * 3.14 * radius * radius + 2 * PI * radious * height
result 0;
}
